// loadsh를 ts로 사용하기 위한 d.ts
// d가 들어가있는 파일은 모듈 사용을 위한 declare
// declare module "lodash" {
//   // 타입을 declare로 선언할 건데
//   interface Lodash {
//     camelCase: (str: string) => string;
//     snakeCase: (str: string) => string;
//   }

//   const _: Lodash;
//   // 언더바 변수로 사용하고 있으니깐
//   export default _;
// }

// 모듈의
